import React from 'react';

const BudgetPlanner = () => {
  return (
    <section id="budget" className="section">
      <div className="container" style={{ textAlign: 'center' }}>
        <h2>Interactive Budget Planner</h2>
        <p style={{ color: 'var(--text-secondary)', marginBottom: '32px' }}>Personalize your financial plan.</p>
        <div style={{ padding: '60px', background: 'var(--bg-card)', borderRadius: '20px' }}>
            <p>Budget Analysis Module Coming Soon</p>
        </div>
      </div>
    </section>
  );
};

export default BudgetPlanner;
