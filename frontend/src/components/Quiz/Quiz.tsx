import React from 'react';

const Quiz = () => {
  return (
    <section id="quiz" className="section">
      <div className="container" style={{ textAlign: 'center' }}>
        <h2>Test Your Knowledge</h2>
        <p style={{ color: 'var(--text-secondary)', marginBottom: '32px' }}>This module is being migrated to the new backend API.</p>
        <div style={{ padding: '60px', background: 'var(--bg-card)', borderRadius: '20px' }}>
            <p>Interactive Quiz Coming Soon</p>
        </div>
      </div>
    </section>
  );
};

export default Quiz;
